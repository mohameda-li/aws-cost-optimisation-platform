# AWS Cost Optimisation Platform

A serverless AWS FinOps automation platform designed to detect and reduce cloud cost waste across ECS, EKS, RDS, S3, and Spot Instances. The web application manages customer onboarding and bundle generation, and customers deploy the generated bundle into their own AWS account with Terraform.

## Repository Layout

`flaskAPP/web`
: Flask web application, admin/customer flows, bundle generation, templates, and static assets.

`src/runner`
: Runner Lambda source. This orchestrates enabled optimisers, aggregates totals, and generates reports.

`src/optimisers`
: Optimiser Lambda source packages for S3, RDS, ECS, EKS, and Spot.

`src/data`
: Pricing reference data used when bundles are generated.

`docker`
: MySQL initialisation scripts for local development.

## Local Development

Start the web app and database with Docker Compose:

```bash
docker compose up --build
```

The web app will be available on `http://127.0.0.1:5050`.

Copy [.env.example](/Users/mohamedali/Library/CloudStorage/OneDrive-UWEBristol/Year%203/DSP/FinalApp/.env.example) to `.env` if you want to override the default local Docker settings.

For email verification and password reset codes, configure the SMTP values in `.env`. Without SMTP, the app falls back to local development behavior and logs or shows the code for testing.

## Tests

Run the automated test suite with:

```bash
python3 -m unittest discover -s tests -v
```

Run coverage with:

```bash
python3 -m coverage run -m unittest discover -s tests
python3 -m coverage report
```

The current automated coverage includes:

- unit tests for optimiser cost, recommendation, and savings logic
- runner helper tests
- report generation tests
- bundle generation integration tests
- Flask authentication and forgot password route tests
- a basic web app smoke test
- admin approval and management route tests
- customer bundle download route tests
- shared helper tests for bundle and customer configuration logic
- multi-application customer account tests
- approval and bundle-generation rollback tests

## Main Product Flows

The current platform supports:

- customer application submission with email verification
- customer login and password reset with verification codes
- customer self-service dashboard for reviewing, editing, withdrawing, and creating multiple applications from one account
- per-application snapshots for organisation name, contact name, and contact email so one application does not overwrite another
- customer to admin conversation threads linked to each application
- guest and customer contact flows, with guest email required before a message can be sent
- admin review dashboard with queue views and filtered application management
- admin application detail pages with decision controls, direct conversation access, and deployment bundle download
- admin contact inbox and recent customer message view
- superuser-only admin account management

## CI

GitHub Actions CI is defined in [.github/workflows/ci.yml](/Users/mohamedali/Library/CloudStorage/OneDrive-UWEBristol/Year%203/DSP/FinalApp/.github/workflows/ci.yml).
It runs:

- the Python test suite
- coverage enforcement with `--fail-under=80`
- `terraform fmt -check`
- `terraform validate`

## Terraform Check

If Terraform is installed locally, validate the customer deployment template with:

```bash
cd flaskAPP/web/package_templates/customer-deployment/terraform
terraform init
terraform validate
```

## Bundle Generation

The customer deployment bundle is generated by the Flask app.

The bundle builder uses the following source-of-truth directories:

- `src/runner`
- `src/optimisers`
- `src/data`

When a bundle is generated, the app:

1. copies the Terraform, config, and docs template
2. writes `terraform.tfvars`
3. writes `customer_config.json`
4. packages `runner.zip`
5. packages only the selected optimiser zips
6. copies only the selected pricing data directories

Customers do not need to zip Lambda files manually.

Bundle access rules:

- admins can generate and download the deployment bundle from the admin application detail page regardless of application status
- customers can download the bundle from their dashboard unless the application is rejected

## Generated Customer Bundle

Each generated bundle contains:

- `terraform/`
- `lambdas/`
- `config/`
- `data/`
- `docs/`
- `README.md`

Customers deploy from the bundle with:

```bash
cd customer-deployment/terraform
terraform init
terraform apply
```

By default, the generated Terraform also triggers one initial report immediately after deployment.

## Reports

The runner produces:

- HTML reports
- JSON reports

Reports are uploaded to the configured S3 bucket under:

```text
reports/<customer>/<run_id>.html
reports/<customer>/<run_id>.json
```

Report notifications are sent directly over SMTP/SES-style email using the configured SMTP values in the generated bundle.
